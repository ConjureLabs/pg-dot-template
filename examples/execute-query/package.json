{
  "name": "execute-query",
  "version": "0.0.0",
  "description": "executes a query, via a prepared template, using pg-dot-template & convenience methods",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "MIT",
  "dependencies": {
    "@conjurelabs/pg-dot-template": "file:../..",
    "pg": "7.17.0"
  }
}
